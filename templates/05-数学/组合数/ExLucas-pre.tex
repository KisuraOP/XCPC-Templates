计算 $\binom{n}{m}\bmod \text{mod}$，不需要保证 $\text{mod}$ 是质数。

设 $\text{mod}=\prod p_i^{k_i}$，$P_i=p_i^{k_i}$，那么时间复杂度 $O(\sqrt{\text{mod}}+\sum P_i\log n)$。显然 $\text{mod}$ 为质数时是最坏情况，时间复杂度 $O(\text{mod}\log n)$。